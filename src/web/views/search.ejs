<!DOCTYPE html>
<html>
	<head>
		<title> Discord bot list FR</title>
		<% include ./partials/head %>
	</head>
	<body>
		<% include ./partials/header %>
		<section class="presentation <%= theme == 1 ? 'presentation-dark' : ''%>" style="background-image: url(./img/pattern.png)">
			<img src="https://cdn.discordapp.com/icons/344955119005073409/3886869684b4d5b197066cfa013b11fb.png?width=512" style="width: auto">
			<div class="text-presentation">
				<div>
					<center>
					<h1 style="font-family: Whitney, Helvetica Neue , Helvetica, Arial, Lucida Grande, sans-serif; text-align: center;">Discord Bot FR</h1>
					<h2 style="font-family: Whitney, Helvetica Neue , Helvetica, Arial, Lucida Grande, sans-serif; text-align: center;">Pimentez votre expérience Discord avec notre liste de bots Discord</h2>
					<div class="input-with-btn">
						<input type="search" placeholder="Rechercher un bot précis" style="font-family: Whitney, Helvetica Neue , Helvetica, Arial, Lucida Grande, sans-serif;">
						<button class="btn2" onclick="search(this)"> Rechercher</button>
						</center>
					</div>
				</div>
			</div>
		</section>
		<div class="content<%= theme == 1 ? '-dark' : ''%>">
			<br>
			<div class="conte is-full" style="background-image: url(../img/patternmid.png)">
				<div class="bot-sect">
					<% if(cursor.length){ %>
					<% for(var i=0; i<cursor.length; i++){%>
					<% var val = cursor[i]; %>
					<div class="card <%= theme == 1 ? 'card-dark' : ''%> card--big" style="opacity: 0.85;">
						<% if(val.avatar){ %>
						<div class="card__image" style="background-image: url(<%=val.avatar%>.png?size=512)">
						</div>
						<%}else {%>
						<div class="card__image" style="background-image: url(/img/kyonna.png)">
						</div>
						<%}%>
						<h2 class="card__title"><%=val.name %> <%if (!val.approved){%>
						<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> red button left" style="float: right;">Non vérifier</span>
						<%} %></h2>
						<div class="card__btn-mid">
							<%if (val.count){ %>
							<!-- A ajouter pour les status en ligne ou pas pour la suite
							<span class="status green">ONLINE</span>-->
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> orange button" style="float: left;padding-left: 5px;padding-right: 4px;">
								<%= val.count %> serveurs
							</span>
							<%} %>
							<%if (val.lib){%>
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> red button" style="padding-left: 5px;padding-right: 4px;float:right;">
								<%= val.lib%>
							</span>
							<%}%>
							<%if (val.status){%>
							<% if (val.status == "online") {%>
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> green button" style="padding-left: 5px;padding-right: 4px">
								<%- val.status%>
							</span>
							<%}else if(val.status == "offline"){%>
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> grey button" style="padding-left: 5px;padding-right: 4px">
								<%- val.status%>
							</span>
							<%}else if (val.status == "dnd"){%>
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> red button" style="padding-left: 5px;padding-right: 4px">
								<%- val.status%>
							</span>
							<%}else if(val.status == "idle"){%>
							<span class="mini ui <%= theme == 1 ? 'inverted' : ''%> orange button" style="padding-left: 5px;padding-right: 4px">
								<%- val.status%>
							</span>
							<%} %>
							
							<%}%>
						</div>
						<% if (val.shortDesc){%>
						<p class="card__text <%= theme == 1 ? 'card__text-dark' : ''%>"><%= val.shortDesc %></p>
						<%}%>
						<div class="card__action-bar <%= theme == 1 ? 'card__action-bar-dark' : ''%>" style="padding-top: 4px;">
							<a href="/bot/<%=val.id%>" class="ui <%= theme == 1 ? 'inverted' : ''%> blue button" style="padding-left: 10px;padding-right: 10px;">Info</a>
							<% if(val.editable) {%>
							<a href="/bot/<%=val.id%>/edit" class="ui <%= theme == 1 ? 'inverted' : ''%> orange button" style="padding-left: 10px;padding-right: 10px;"> Editer</a>
							<%}%>
							<a href="<%=val.invite%>" class="ui <%= theme == 1 ? 'inverted' : ''%> blue button" style="padding-left: 10px;padding-right: 10px;float: right;">Inviter</a>
						</div>
					</div>
					<%}%>
					
					<% }else{%>
					<div class="col-12" style="width: 100%; margin-bottom: 10px;">
						<article class="message is-primary" style="text-align: center">
							<div class="message-header" style="background-color: #374147">
								<strong>Aucun bot trouver :(</strong>
							</div>
							<div class="message-body">
								
								<p>Aucun bot trouver pour la recherche de <strong><%= query%></strong>
								<br>Si vous ne trouver pas, le bot que vous voulez. Ajouter le votre </p>
							</div>
						</article>
					</div>
					<% } %>
				</div>
			</div>
		</div>
	</div>
	<% include ./partials/footer %>
	<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mustache.js/2.3.0/mustache.min.js"></script>
	<script type="text/javascript">
	function search(el) {
	location.href = "/search?username="+el.parentNode.querySelector('input').value
	}
	
	document.querySelectorAll('input[type="search"]').forEach(function(input){
	input.addEventListener('keyup', function(ev){
	if (ev.keyCode == 13) search(ev.target)
	})
	})
	
	
	</script>
</body>
</html>